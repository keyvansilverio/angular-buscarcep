apiVersion: v1
kind: Service
metadata:
    name: keyvansilverioangularbuscarcep
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: keyvansilverioangularbuscarcep